[build]
  command = "npm run build"
  publish = ".output/public"

# Send all requests to Nuxt server (SSR).
# Static assets in .output/public are served directly by Netlify before redirects.
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/server"
  status = 200